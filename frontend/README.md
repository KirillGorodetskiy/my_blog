# Frontend

Independent Next.js 16 prototype for the Kirill blog/portfolio.

The Django app under `backend/` is unchanged and still runs on its own.

## Scripts

```bash
cd frontend
npm install
npm run dev
npm test
npm run build
npm start
```

Dev server: http://localhost:3000
